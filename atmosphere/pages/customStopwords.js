// defining custom stopwords that we need filtered out
export const customStopwords = ['so', 'know', 'however', 'actually', 'instead', 'let', 'just', 'use', 'want', 'will', 'when', `don\'t`, '-', `can\'t`, 'not'];

export default customStopwords;